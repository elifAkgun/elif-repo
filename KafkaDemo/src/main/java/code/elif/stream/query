SELECT
  P.CATEGORY,
  MIN(P.PRODUCT_ID) AS PRODUCT_ID,
  MAX(P.DISCOUNT) AS MAX_DISCOUNT
FROM
  PRODUCT P
WHERE
  P.DISCOUNT = (
    SELECT
      MAX(DISCOUNT)
    FROM
      PRODUCT
    WHERE
      CATEGORY = P.CATEGORY
  )
GROUP BY
  P.CATEGORY
ORDER BY
  P.CATEGORY ASC;